[project]
name = "genesis-oracle"
version = "0.1.0"
description = "Physics simulation engine for exploring standard and dimensionless systems"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["numpy", "matplotlib", "scipy"]
